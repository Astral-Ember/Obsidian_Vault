回调函数

1. 什么是回调函数
2. 如何使用回调函数
3. 回调函数对我们的意义
    1. 延迟执行函数逻辑
    2. 让回调执行的逻辑可以变化

回调函数(CallBack Function)  
是通过函数指针将一个函数的地址传递给另一个函数  
并在合适的时机调用该函数，它通常用于延迟执行某些逻辑
 
说人话：  
回调函数就是一个函数作为参数传递给另外一个函数，并在后者执行过程中调用前者的一种方式  
先记录某一个函数，过一会儿再调用，相当于一会儿回头再调用的函数
 
|   |
|---|
|void doSomthing(void (*func_ptr)())  <br>{  <br>cout \<\< "this is a logic" \<\< '\n';  <br>//执行传入的回调函数  <br>func_ptr();  <br>}<br><br>  <br><br>void myCallbackfunc()  <br>{  <br>cout \<\< "this is a callback function" \<\< '\n';  <br>}  <br>int main()  <br>{  <br>cout \<\< "回调函数\n";  <br>//传入的该函数，就可以称为回调函数，因为它会在另一个函数中被调用  <br>doSomthing(myCallbackfunc);  <br>}|

e.g.如果在游戏中有一个怪物死亡了，可以通知不同的地方执行不同的逻辑（怪物数量，计分板等）