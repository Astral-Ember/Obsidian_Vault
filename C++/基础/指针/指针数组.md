指针数组

1. 知识回顾
2. 指针数组的基本概念
3. 指针数组的使用

[数组指针](指针和二维数组.md)  
数组指针是我们在学习指针和数组中 二维数组相关知识时学习的  
数组指针是一个指向整个数列的指针，本质上是一个指针，指向一个包含 n 元素的数组的指针  
数列指针定义规则：变量类型（*指针名）[元素个数]  
int (*p2)[4] 表示指向包含4个 int 元素的一维数组的指针  
指针数组是由 n 个指针组成的数组，每个指针可以指向一个独立的元素地址  
定义规则：  
变量类型* 数组名[数组长度]；
 
重点：  
指针数组中每个元素存储的是地址  
想要获取地址中的值，需要加*  
e.g.

|   |
|---|
|int* p[4];  <br>int a = 10;  <br>p[0] = &a;  <br>cout \<\< *p[0] \<\< endl;//10|
 
|   |
|---|
|//声明  <br>int a = 10, b = 20, c = 30; int* q = &a;  <br>int* p[] = { &a, &b, &c };  <br>//查找地址  <br>cout \<\< p[0] \<\< " " \<\< p[1] \<\< " " \<\< p[2] \<\< endl;  <br>//查找地址对应的值  <br>cout \<\< *p[0] \<\< " " \<\< *p[1] \<\< " " \<\< *p[2] \<\< endl;//10 20 30  <br>//修改指向地址  <br>int d = 40;  <br>p[0] = &d;  <br>cout \<\< *p[0] \<\< " " \<\< *p[1] \<\< " " \<\< *p[2] \<\< endl;//40 20 30  <br>//修改值  <br>*p[0] = 99;  <br>cout \<\< *p[0] \<\< " " \<\< *p[1] \<\< " " \<\< *p[2] \<\< endl;//99 20 30  <br>cout \<\< d \<\< endl;//99  <br>//遍历  <br>//容量  <br>cout \<\<"容量：" \<\< sizeof(p) / sizeof(p[0]) \<\< '\n';//3  <br>for(int i=0;i\< sizeof(p) / sizeof(p[0]);i++)  <br>{  <br>cout \<\< *p[i] \<\< " ";  <br>}  <br>cout \<\< '\n';  <br>//地址关系  <br>cout \<\< p \<\< '\n'; //数组首地址  <br>cout \<\< &p[0] \<\< '\n'; //第一个元素地址 它们俩一样的  <br>cout \<\< *p \<\< '\n'; //指针数组第一个元素的值 也就是变量a的地址  <br>cout \<\< &d \<\< '\n'; //变量d的地址 和上面那个值是一样的  <br>//可以这样理解：p=&p[0], *p=p[0],p[0]=&d,*p[0]=d|