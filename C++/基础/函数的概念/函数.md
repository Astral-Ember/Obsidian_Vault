函数

1. 基本概念
2. 函数在哪声明？
3. 基本语法
4. 实际运用
    ![Exported image](Exported%20image%2020251125135916-0.png)2. 无参无返回值函数
    3. 有参无返回值函数
    4. 无参有返回值函数
    5. 有参有返回值函数
5. ==由于我采用了头文件和源文件分开的形式，代码== ==对应如下==
    ![Exported image](Exported%20image%2020251125135919-1.png)2. FileName.cpp
    3. function.h
    4. function.cpp

函数（方法） 本质是一块具有名称的代码块  
可以使用函数（方法）的名称来执行该代码块  
函数（方法）是封装代码进行重复使用的一种机制
 
函数（方法）的主要作用

1. 封装代码
2. 提升代码复用率（少写点代码）
3. 抽象行为

标准的做法是：  
函数的声明一般在头文件(.h)  
函数的定义一般在源文件(.cpp)  
目前还没有学习头文件和源文件的知识  
因此直接将函数声明和定义在源文件(.cpp)中  
==注意：==  
如果在源文件中声明和定义函数，我们需要确保在使用之前函数已经被声明

|   |
|---|
|返回类型 函数名 （参数列表）  <br>{  <br>函数的代码逻辑；  <br>函数的代码逻辑；  <br>函数的代码逻辑；<br><br>  <br><br>return 返回值；（如果有返回值的话）  <br>}  <br>返回类型：函数执行后返回的数据类型，如果没有返回值则使用 void。  <br>14种变量类型（int、float、double、char、bool、string）+复杂数据类型（数组、枚举、结构体、指针）  <br>void关键字 表示函数没有返回值。|

声明与定义（声明要在main函数前）

|   |
|---|
|void functionInFileName()  <br>{  <br>std::cout \<\< "functionInFileName called\n";  <br>}|

使用

|   |
|---|
|int main()  <br>{  <br>functionInFileName(); // 调用函数（定义在 function.cpp）  <br>}|

声明与定义

|   |
|---|
|void sayHello(std::string txt)  <br>{  <br>std::cout \<\< "Hello, " \<\< txt \<\< "\n";  <br>}|

使用

|   |
|---|
|int main()  <br>{  <br>//functionInFileName(); // 调用函数（定义在 function.cpp）  <br>sayHello("World"); // 调用另一个函数（定义在 function.cpp）  <br>}|

声明与定义

|   |
|---|
|std:: string OutPutName()  <br>{  <br>return "function.cpp";  <br>}|

使用

|   |
|---|
|cout \<\< OutPutName() \<\< '\n'; // 调用返回文件名的函数（定义在 function.cpp）|

声明与定义

|   |
|---|
|int sum(int a, int b)  <br>{  <br>return a + b;  <br>}|

使用

|   |
|---|
|int result = sum(3, 5); // 调用求和函数（定义在 function.cpp）  <br>cout\<\<"Sum: " \<\< result \<\< '\n';|

|   |
|---|
|#include \<iostream\>  <br>#include "function.h" // 引入同一目录下的头文件<br><br>  <br><br>using namespace std;<br><br>  <br><br>int main()  <br>{  <br>functionInFileName(); // 调用函数（定义在 function.cpp）  <br>sayHello("World"); // 调用另一个函数（定义在 function.cpp）  <br>cout \<\< OutPutName() \<\< '\n'; // 调用返回文件名的函数（定义在 function.cpp）  <br>int result = sum(3, 5); // 调用求和函数（定义在 function.cpp）  <br>cout\<\<"Sum: " \<\< result \<\< '\n';  <br>}|

|   |
|---|
|#pragma once  <br>#include \<string\><br><br>  <br><br>void functionInFileName();  <br>void sayHello(std::string txt);  <br>std:: string OutPutName();  <br>int sum(int a, int b);|

|                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #include "function.h"  <br>#include \<iostream\><br><br>  <br><br>void functionInFileName()  <br>{  <br>std::cout \<\< "functionInFileName called\n";  <br>}<br><br>  <br><br>void sayHello(std::string txt)  <br>{  <br>std::cout \<\< "Hello, " \<\< txt \<\< "\n";  <br>}  <br>std:: string OutPutName()  <br>{  <br>return "function.cpp";  <br>}  <br>int sum(int a, int b)  <br>{  <br>return a + b;  <br>} |