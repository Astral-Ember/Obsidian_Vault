auto变量

1. 变量的存储类型是什么意思
2. auto变量

声明变量时，在前面加上 auto、static、register、extern 四种存储类型关键词 它们决定了变量的存储位置、生命周期、作用域以及默认的初始值 就好像常量关键字 const 一样，可以让变量有一些特殊的使用规则  
自动变量(使用时一定要赋值）  
作用：  
让编译器自动推断变量的类型  
生命周期：  
和变量的规则一致  
优点：  
可以减少手动定义类型的麻烦，提高代码的简洁性  
缺点：  
影响代码可读性，必须在声明时进行初始值，否则编译器无法推断类型  
注意：  
在初学阶段，不太建议频繁使用 auto，因为不太有利于查看逻辑  
e.g.

|   |
|---|
|int i = 10;  <br>auto j = 11;  <br>cout \<\< j \<\< endl;  <br>auto str = "hello";  <br>cout \<\< str \<\< endl;  <br>auto k = 3.14;  <br>cout \<\< j + k \<\< endl;|