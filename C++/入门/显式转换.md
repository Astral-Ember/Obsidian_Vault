显式转换

1. 什么是显示转换
2. 括号强转
3. 其他类型转字符串 to_string方法
4. 字符串转其他类型 stoxx 方法

手动处理，强制转换  
目的：将高精度的类型转换为 低精度  
语法：变量类型 变量名=（变量类型）变量 或（变量类型）（表达式）  
注意：精度问题 范围问题  
作用：括号强转最大的作用是，我们可以在计算过程中强制改变隐式转换规则  
需要一个新的头文学（系统写好的内容）  
#include\<string\>  
使用时需要引用std命名空间  
提供了一个系统写好的方法： to_string（整型变量）  
虽然没有明确支持 short bool char 相关变量  
但是由于方法参数也存在隐式转换 会默认转换到最接近的类型进行转换处理  
需要一个新的头文学（系统写好的内容）  
#include\<string\>  
使用时需要引用std命名空间  
字符串转int -\>stoi（字符串）

|   |
|---|
|e.g. int i = stoi(str);|

字符串转long -\>stol（字符串）  
字符串转long long -\>stoll（字符串）  
字符串转unsigned long -\>stoul（字符串）  
字符串转unsigned long long -\>stoull（字符串）  
注意：  
转换失败会报错  
原因：溢出 和 类型不匹配