endl 和 \n 的**区别**

|   |   |   |
|---|---|---|
|**对比项**|**endl**|**\n**|
|**作用**|输出换行并**刷新输出缓冲区**|只输出换行符，不刷新缓冲区|
|**性能**|较慢，因为会强制刷新缓冲区|更快，因为不刷新|
|**本质**|是一个**操纵符**（stream manipulator）|是一个普通的**字符**|
|**头文件**|\<iostream\>|不需要额外头文件|
|**使用场景**|输出重要信息后需要立刻显示（如日志、调试输出）|普通输出、频繁打印时（性能更好）|