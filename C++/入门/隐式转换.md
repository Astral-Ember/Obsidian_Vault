隐式转换

1. 什么是类型转换和隐式转换
2. 赋值运算中的隐式转换
    
    当使用 = 时  
    A = B  
    当A、B类型不匹配时  
    C++会尝试将右侧的值转换为左侧变量的类型
    
    2. 低精度转换高精度
    
    通常不会有问题 不会丢失数据 也不会出现未定义行为
    
    |   |
    |---|
    |int i;  <br>char c = 'A';  <br>short s = 3;  <br>bool b = true;  <br>i = c;  <br>cout \<\< i \<\< '\n';  <br>i = s;  <br>cout \<\< i \<\< '\n';  <br>i = b;  <br>cout \<\< i \<\< '\n';|
    
    5. 高精度转换低精度
    
    可能会出现精度丢失，尽量避免这样用
    
    7. string类型与其他类型之间不存在隐式转换
    8. 无符号和有符号之间的隐式转换
    
    同样遵循以上规则，但是把一个负数用无符号类型装载，得到的结果不准确，不要这样使用
    

类型转换 是不同变量类型之间的相互转换  
隐式转换 被称为自动类型转换，由编译器自动执行，用于将一种数据类型的值转换为另一种数据类型