1. 知识点回顾：[C++](结构体嵌套，大小，内存对齐.md)基础中结构体的内存占用相关知识点
2. 类对象的内存占用
3. 空类型所占字节数

类对象的内存占用计算规则和结构体一致  
同样可以通过sizeof过去其占用的字节数  
注意：  
包含虚函数时 需要加上虚函数表指针的大小  
如果一个类型（类或结构体）中不声明确任意成员变量  
那么它相当于是一个空类型  
这时我们单独用 sizeof 计算其占用内存空间，将会得到 1 个字节  
主要原因：  
确保对象具有唯一地址  
C++ 中的对象需要内存地址，即使对象没有任何成员变量，它仍然需要一个唯一的内存地址  
注意：  
当某类型被继承时，只要子类中有成员变量，则会优化掉空类型的空间，不会再增加