多继承时的同名虚函数

1. 知识回顾
2. 多继承时的同名虚函数
3. 内部原理

[多重继承时的同名成员问题](多重继承基本规则.md)  
如果多个父类中存在同名成员，子类中需要通过 父类名::成员 的方式 明确作用域  
如果子类声名了同名方法，则可以直接点击出
 
多重继承时，如果多个父类中存在同名函数（函数名、参数列表完全一致），子类只需要重写一次，便可统一覆盖所有父类版本的虚函数  
两个虚函数表中的函数指针指向的都是子类重写的函数