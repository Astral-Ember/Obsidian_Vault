类和对象

1. 什么是类
2. 类在哪里声明和定义
3. 类声明的语法
4. 类声明实例
5. 什么是（类）对象
6. 实例化对象的基本语法
7. 实例化对象
8. 总结

基本概念  
具有相同特征  
具有相同的行为  
一类事物的抽象  
类是对象的模板  
可以通过类创建出对象
 
类的关键词  
class
 
类一般  
在 .h 结尾的头文件中声明  
在 .cpp 结尾的源文件中定义  
对于小型类或者模板类，可以在头文件中直接完成声明和定义  
因此我们一般需要新建脚本文件

![Exported image](Exported%20image%2020251125140159-0.png)

和[结构体](结构体基本用法.md)声明非常类似 只不过会有一些新的知识点  
以Person.h为例

|   |
|---|
|#pragma once  <br>#include\<iostream\>  <br>using namespace std;  <br>class Person  <br>{  <br>//访问修饰符  <br>public:  <br>//第一部分  <br>//成员变量  <br>int age;  <br>bool sex;  <br>string name;  <br>//第二部分  <br>//构造函数和析构函数（可选）  <br>Person();  <br>~Person();  <br>//第三部分  <br>//成员函数（方法）  <br>void run();  <br>void eat();  <br>};|

基本概念  
类别的声明 和 类对象(变量)声明是两个概念  
类别的声明 类似 枚举 和 结构体的声明 类的声明相当于声明了一个自定义变量类型  
而对象 是类创建出来的  
相当于声明一个指定类的变量  
类创建对象的过程 一般称为实例化对象  
步骤一：引用类头文件  
在Person.cpp中

![Exported image](Exported%20image%2020251125140201-1.png)

步骤二：声明对象  
普通对象（栈上）  
类名 变量名；  
类名 变量名 = 构造函数名（…）；  
指针对象（堆上）  
类名* 变量名 = new 构造函数名（…）；  
引用对象（根据引用对象自己决定在栈还是堆）  
类名& 变量名 = 想要引用的对象；

|   |
|---|
|#include "Person.h"  <br>#include \<iostream\>  <br>using namespace std;  <br>int main()  <br>{  <br>Person p;  <br>Person p2 = Person();  <br>Person* p3 = new Person();  <br>delete p3;  <br>p3 = nullptr;  <br>Person& p4 = p;  <br>}|

类和对象相关知识点 和我们的结构体中的结构体和对象 几乎一致  
类和结构体 都是我们自定义的变量类型 数据和方法的集合  
他们更像是 对象的模板 是形容（抽象）一类对象的代码  
如果要真正使用它们，我们要进行实例化 也就是创建对象
 
注意：  
类的声明和定义可以在.h和.cpp中分开进行  
类的声明中多了一些新的知识点  
在使用别的文件中声明和定义的类时，需要先引用对应的头文件 否则无法正常使用类