友元类

1. 友元类的概念
2. 友元类的使用

友元类是允许一个类访问另一个类的私有(private)和保护(protected)成员  
友元类的作用类型类似于友元函数，但是它赋予了整个类访问权限，而不仅限于某个函数
 
规则：  
在一个类中利用 friend 关键字让一个类成为该类的友元  
friend class 另一个类名;  
另一个类成为了另一个类的友元类后，就可以在其中访问另一个类的私有或保护成员  
注意：  
友元关系不能逆向，除非互为友元  
友元关系不能传递，即父类为一个类的友元，它的子类并不继承这个关系