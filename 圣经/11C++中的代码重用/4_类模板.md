## 1.类模板的使用

### 1、为什么需要类模板？
类模板与[[5_函数模板|函数模板]]的定义和使用类似，有时，有两个或多个类，其功能是相同的，仅仅是数据类型不同，我们可以通过如下面语句声明了一个类模板：
```
template <typename T>
class A
{
public:
	A(T t){
		this->t = t;
	}
 
	T& getT(){
		return t;
	}
 
public:
	T t;
};
```
