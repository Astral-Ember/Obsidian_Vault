C++没有提供二维数组类型，但用户可以创建每个元素本身都是数组的数组。例如，假设要存储5个城市在4年间的最高温度。在这种情况下，可以这样声明数组：
`int maxtemps [4][5];`
该声明意味着maxtemps是一个包含4个元素的数组，其中每个元素都是一个由5个整数组成的数组表达式`maxtemps[0]`是maxtemps数组的第一个元素，因此`maxtemps[0]`本身就是一个由5个int组成的数组。`maxtemps[0]`数组的第一个元素是`maxtemps [0][0]`，剩下的以此类推

## 1.初始化二维数组

对于二维数组来说，由于每个元素本身就是一个数组，因此可以使用与上述代码类似的格式来初始化每一个元素。因此,初始化由一系列逗号分隔的一维数组初始化(用花括号括起)组成：
```
int maxtemps [4] [5] = // 2-D array
{
	{96, 100, 87, 101, 105},// values for maxtemps [0]
	{96, 98, 91, 107, 104},// values for maxtemps [1]
	{97, 101, 93, 108, 107},// values for maxtemps [2]
	{98, 103, 95, 109, 108}// values for maxtemps [3]
};
```

## 2.使用二维数组
